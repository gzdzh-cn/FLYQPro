# FlyQPro 桌面端打包命令

本文记录 FlyQPro 桌面端各平台的构建命令。所有命令都在本文件所在的 `FlyQPro-wails3` 项目目录执行。

## 环境要求

- Go 1.25 或更高版本
- Node.js 和 npm
- Wails 3 beta，能够执行 `wails3 task`
- macOS 构建需要 Xcode Command Line Tools
- Windows NSIS 构建需要 NSIS 和 WebView2 引导器环境
- Windows MSI 构建需要 WiX Toolset v4
- Linux 构建需要 GTK、WebKitGTK、AppImage 和 nfpm 相关依赖

Windows、Linux 安装包建议在对应操作系统上构建和验证。

## 构建前同步版本资源

修改以下版本来源：

```text
internal/version/version.go
build/config.yml
```

然后执行：

```bash
wails3 task common:update:build-assets
```

该命令只同步 macOS、Windows、Linux 的打包元数据，不会生成安装包。确认生成的 `build/darwin/Info.plist`、`build/windows/info.json` 和 `build/windows/nsis/wails_tools.nsh` 版本正确后，再执行平台打包命令。

## 各平台构建命令

首次构建 Windows MSI 时，先在 Windows 安装 WiX Toolset v4：

```powershell
dotnet tool install --global wix
wix --version
```

| 平台 | 执行环境 | 构建命令 | 产物 | 说明 |
| --- | --- | --- | --- | --- |
| macOS Apple Silicon | macOS | `go test ./...`<br>`wails3 task darwin:package:macdmg ARCH=arm64`<br>`mv bin/FlyQPro.dmg bin/FlyQPro-macos-arm64.dmg` | `bin/FlyQPro-macos-arm64.dmg` | Apple Silicon 原生 DMG。每次构建都会先生成并覆盖 `bin/FlyQPro.dmg`，完成后立即改名。 |
| macOS Intel | macOS | `go test ./...`<br>`wails3 task darwin:package:macdmg ARCH=amd64`<br>`mv bin/FlyQPro.dmg bin/FlyQPro-macos-x86_64.dmg` | `bin/FlyQPro-macos-x86_64.dmg` | Intel 原生 DMG。当前产物未进行 Developer ID 签名和 Apple 公证。 |
| Windows x64 NSIS | Windows x64 | `wails3 task windows:package:installer ARCH=amd64` | `bin/FlyQPro-amd64-installer.exe` | NSIS `.exe` 安装包，包含 Wails 的 WebView2 引导器，普通用户优先使用。 |
| Windows ARM64 NSIS | Windows ARM64 | `wails3 task windows:package:installer ARCH=arm64` | `bin/FlyQPro-arm64-installer.exe` | NSIS `.exe` 安装包，包含 Wails 的 WebView2 引导器。 |
| Windows x64 MSI | Windows x64 | `wails3 task windows:package:msi ARCH=amd64 VERSION=0.1.1` | `bin/FlyQPro-amd64.msi` | WiX `.msi` 安装包，适合系统部署；不包含 WebView2 引导器。 |
| Windows ARM64 MSI | Windows ARM64 | `wails3 task windows:package:msi ARCH=arm64 VERSION=0.1.1` | `bin/FlyQPro-arm64.msi` | WiX `.msi` 安装包，适合系统部署；不包含 WebView2 引导器。 |
| Linux x64 | Linux x64 | `go test ./...`<br>`PRODUCTION=true wails3 task linux:package ARCH=amd64`<br>`ls -lh bin` | `bin/` 下的 AppImage、deb、rpm 和 Arch Linux 包 | 执行前检查 `build/linux/nfpm/nfpm.yaml` 的包名、版本、二进制路径、图标和桌面文件路径。 |

Windows 也可以使用通用任务 `wails3 task windows:package ARCH=amd64`，它会调用 Windows 的 `package` 任务并生成 NSIS 安装包。发布脚本建议使用带 `installer` 后缀的命令，以明确指定 NSIS。

NSIS 和 MSI 是两套独立安装包，不需要同时安装。NSIS 会尝试引导安装 WebView2；MSI 只安装应用本身，目标设备需要预先安装 WebView2 Runtime。`VERSION` 用于设置 MSI 的产品版本，发布新版本时应同步修改。

## 构建后检查

```bash
git diff --check
go test ./...
```

逐个平台检查安装、启动、卸载、局域网发现、聊天、文件传输和共享文件夹功能。构建产物不要提交到 Git，签名密钥、签名密码和 GitHub Token 也不要提交。
