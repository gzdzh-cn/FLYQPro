package version

// AppVersion is the single application version exposed to the frontend and
// used by the desktop service. Keep this value in sync with the packaging
// metadata in build/config.yml when releasing a new version.
const AppVersion = "0.1.0"
